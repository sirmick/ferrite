//! Diagnostic: read a 48 kHz stereo s16 WAV (left = ch A, right = ch B)
//! and run it through `RtlAis`. Stub for now — fleshed out alongside
//! capture-ais.json once we have a fixture to point at.

fn main() {
    eprintln!("analyze-ais-wav: not yet implemented");
    std::process::exit(2);
}
