/* Shim for <Viewer.h> — FLTK multi-channel viewer window. The only
   symbol the modems use, dlgViewer, is declared in shim/fl_digi.h.
   Nothing else here is on the RX path. */
#ifndef FERRITE_FLDIGI_SHIM_VIEWER_H
#define FERRITE_FLDIGI_SHIM_VIEWER_H
#include "fl_digi.h"
#endif
