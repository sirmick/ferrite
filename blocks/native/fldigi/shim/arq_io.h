/* Shim stub for <arq_io.h> — not on the RX decode path. Grow only
   if a compiler error proves a real dependency. */
#ifndef FERRITE_FLDIGI_SHIM_ARQ_IO_H
#define FERRITE_FLDIGI_SHIM_ARQ_IO_H
#endif
