/* Shim for <confdialog.h> — the entire FLTK preferences dialog. The
 * modem DSP includes it only for progdefaults (declared via the
 * vendored configuration.h). Nothing else here is on the RX path. */
#ifndef FERRITE_FLDIGI_SHIM_CONFDIALOG_H
#define FERRITE_FLDIGI_SHIM_CONFDIALOG_H
#include "configuration.h"
#endif
