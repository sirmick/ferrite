{
  "name": "ferrite",
  "version": "0.0.1",
  "private": true,
  "description": "Ferrite SDR — root workspace (web, packages/*, headless, tools/*).",
  "license": "MIT OR Apache-2.0",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20.10.0",
    "pnpm": ">=10"
  },
  "scripts": {
    "lint": "pnpm -r lint",
    "check": "pnpm -r check",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "lefthook": "^1.8.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "@swc/core", "lefthook"]
  }
}
