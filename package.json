{
  "name": "ferrite",
  "version": "0.9.0",
  "private": true,
  "description": "Ferrite SDR — root workspace (web, tools/*).",
  "license": "MIT OR Apache-2.0",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20.10.0",
    "pnpm": ">=10"
  },
  "scripts": {
    "lint": "pnpm -r lint",
    "check": "pnpm -r check",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "prepare": "lefthook install || echo 'lefthook install skipped (likely no prebuilt for this arch — pre-commit hooks unavailable, build/run still works)'"
  },
  "devDependencies": {
    "lefthook": "^1.8.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "@swc/core", "lefthook"]
  }
}
