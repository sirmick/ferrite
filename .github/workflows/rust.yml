name: Rust

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: rust-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Install system deps
        # libsoapysdr-dev: ferrited + the `soapysdr` feature on
        # ferrite-blocks link against libSoapySDR via pkg-config.
        # clang / lld / wasi-libc: vendored multimon-ng + ft8_lib
        # cross-compile to wasm32 via clang, link against wasi-libc's
        # libc.a — needed for the `cargo check (wasm32)` step below.
        # python3-{soundfile,scipy,numpy}: feed samples/sigidwiki/
        # convert.py for the e2e fixtures step below.
        run: |
          sudo apt-get update
          sudo apt-get install -y libsoapysdr-dev clang lld wasi-libc \
            python3-soundfile python3-scipy python3-numpy

      - name: Convert sigidwiki fixtures to 22050 Hz mono WAV
        # eas / packet / pager e2e tests load files from
        # samples/sigidwiki/22050_mono/, which is .gitignore'd
        # (the WAVs are regenerated from the upstream MP3s). Run the
        # convert script before cargo test so the tests find their
        # inputs.
        run: python3 convert.py
        working-directory: samples/sigidwiki

      - name: Install stable toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
          targets: wasm32-unknown-unknown

      - uses: Swatinem/rust-cache@v2

      - name: cargo fmt --check
        run: cargo fmt --all -- --check

      - name: cargo clippy
        # Workspace sets `pedantic = warn` in Cargo.toml as guidance for
        # devs; CI denies the default lint set but keeps pedantic
        # informational (avoids new pedantic lints in stable Rust
        # breaking CI overnight).
        run: cargo clippy --workspace --all-targets -- -D warnings -A clippy::pedantic

      - name: cargo test
        run: cargo test --workspace --all-targets

      - name: cargo check (wasm32)
        run: cargo check -p ferrite-blocks --target wasm32-unknown-unknown --features wasm
