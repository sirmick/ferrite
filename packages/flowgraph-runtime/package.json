{
  "name": "@ferrite/flowgraph-runtime",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Environment-agnostic flowgraph runtime: JSON parse, validate, instantiate, wire, schedule.",
  "license": "MIT OR Apache-2.0",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "lint": "prettier --check src",
    "check": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "build": "tsc --emitDeclarationOnly"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
