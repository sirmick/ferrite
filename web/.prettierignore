build
.svelte-kit
node_modules
pnpm-lock.yaml
src/lib/wasm/blocks
src/lib/wasm/runtime
# Generated Emscripten module (blocks/native/fldigi/emscripten/build.sh);
# fldigiBridge.ts in the same dir is hand-written and stays linted.
src/lib/wasm/fldigi/fldigi.mjs
# Same story for the generated whisper STT Emscripten module.
src/lib/wasm/whisper/whisper.mjs
# Vendored upstream band plan (Arrin-KN1E/SDR-Band-Plans, CC0) — keep
# byte-for-byte so upstream re-syncs are a one-file copy with no
# formatter drift.
src/lib/presets/bandplan-usa.json
# Generated from the Rust single source of truth
# (tools/ferrite-ctl/src/sdr_tables.rs) by `cargo run -p ferrite-ctl --
# gen-tables`; its byte shape is pinned by a Rust CI equality test, so
# prettier must not reflow it.
src/lib/controls/if-filter-ladders.generated.json
# Vendored Natural Earth polygons (public domain) — used by StationMap;
# keep byte-for-byte so upstream re-syncs are a one-file copy with no
# formatter drift.
static/map/*.geojson
# Local dev server log dropped by run.sh
dev-server.log
